<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty scope="context" name="appName"
                    source="spring.application.name" defaultValue="horizonai-backend"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>UTF-8</charset>
            <pattern>time=%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} level=%-5level app=${appName} traceId=%X{traceId:-none} taskId=%X{taskId:-none} thread="%thread" logger=%logger{36} message="%msg"%n%ex</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
